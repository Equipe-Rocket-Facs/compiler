// Caso de teste de erro n° 2, múltiplos erros de sintaxe e lógica

programa

// Erro: Variável contador já foi declarada
inteiro contador
inteiro contador

// Erro: Operador lógico inválido e variáveis não declaradas
if (x >>> y) {
    escreva("teste")
}

// Erro: Condição do while com formato impróprio
while x < 10 { // Faltam parênteses
    contador = contador + 1
}

// Erro: Declaração fora de escopo e variável não inicializada
inteiro teste
escreva(teste + 1)

// Erro: Fechamento incoerente do programa
fimprogram // Escrito incorretamente